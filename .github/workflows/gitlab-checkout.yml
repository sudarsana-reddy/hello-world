name: Checkout Gitlab Code and build



# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on: [workflow_dispatch]
      
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "getPegaPipelines"
  getPegaPipelines:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - name: Checkout
      run: |
        git clone https://oauth2:glpat-ztn95j3-rcszw-8ApkwW@gitlab.com/sudarsana1/freecodecamp-gitlab-ci.git
        ls
        cd freecodecamp-gitlab-ci
        ls
    

